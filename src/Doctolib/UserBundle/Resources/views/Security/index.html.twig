{% if error %}
    {# message d'erreur récupérable dans error.message #}

    {{ error.message }}
{% endif %}
<form action="{{ path('login_check') }}" method="POST">
    <input type="email" name="_username" placeholder="votre email" value="{{ last_username }}">
    <input type="password" name="_password"  placeholder="votre mot de passe">
    <input type="submit" value="connexion">
</form>